Implement an e-commerce application's APIs using PostgreSQL.

Category
Brand
Product
Customer
Orders




        for(let userRole of userRoles){
            if(roles.includes(userRole.name))
                hasRole = true;
                break;
        }
